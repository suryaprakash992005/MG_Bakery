import React from 'react';

export const FloatingCartButton: React.FC = () => {
  // MobileBottomNav handles the shopping bag icon, live count badge, and
  // click-to-open cart drawer on mobile/tablet viewports (< 1024px).
  // Desktop viewports (>= 1024px) use the Navbar cart icon.
  return null;
};
